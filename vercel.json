{
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/scoring/:path*", "destination": "/api/engine" },
    { "source": "/api/:path*",         "destination": "/api/server" }
  ]
}
